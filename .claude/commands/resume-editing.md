---
description: Switch back to editing mode from advisory mode with safety checks
---

Switch back to editing mode from advisory mode.

Before resuming:
1. Read UV_Leakage_Geometry/HANDOFF.md to catch up on project state
2. Run `git status` to check for any uncommitted changes
3. Warn the user if there are uncommitted changes that might conflict with planned work

Then confirm to the user that editing mode is active and summarize the current project state in 2-3 sentences.
